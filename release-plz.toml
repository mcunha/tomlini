# release-plz configuration
# https://release-plz.ieni.dev/

[workspace]
changelog_update = true

# Don't publish these crates
[[package]]
name = "toml_benchmarks"
publish = false

# Publish order — tomlini first (tomlini_serde depends on it).
[[package]]
name = "tomlini"
publish = true
[[package]]
name = "tomlini_serde"
publish = true
